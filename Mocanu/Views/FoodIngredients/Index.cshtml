@model IEnumerable<Mocanu.Models.LModels.FoodIngredient>

@{
	ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
	@Html.ActionLink("Create New", "Create")
</p>
<table class="table">
	<tr>
		<th>
			@Html.DisplayNameFor(model => model.Ingredient)
		</th>
		<th></th>
	</tr>

	@foreach (var item in Model)
	{
		<tr>
			<td>
				@Html.DisplayFor(modelItem => item.Ingredient)
			</td>
			<td>
				@Html.ActionLink("Edit", "Edit", new { id = item.FoodIngredientId }) |
				@Html.ActionLink("Details", "Details", new { id = item.FoodIngredientId }) |
				@Html.ActionLink("Delete", "Delete", new { id = item.FoodIngredientId })
			</td>
		</tr>
	}

</table>
